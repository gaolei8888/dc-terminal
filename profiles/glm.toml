name = "glm"
command = ["claude", "--dangerously-skip-permissions"]
is_agent = true
# 干活时底部一定有「(12s · esc to interrupt)」。
# 不能拿 `? for shortcuts` 当 idle_pattern：上面这条命令带的
# `--dangerously-skip-permissions` 会让底栏常驻「bypass permissions on」，
# 正好把那行顶掉——profile 会用自己的启动参数保证自己永远判不出空闲。
busy_pattern = "esc to interrupt"
error_pattern = "API Error|Connection closed mid-response"

[label]
zh = "GLM"
en = "GLM"

[note]
zh = "智谱，套用 Claude 界面"
en = "Zhipu AI, through the Claude interface"

[env]
ANTHROPIC_BASE_URL = "https://open.bigmodel.cn/api/anthropic"

[secret]
env = "ANTHROPIC_AUTH_TOKEN"
url = "https://open.bigmodel.cn/usercenter/apikeys"

[secret.hint]
zh = "在 open.bigmodel.cn 的用户中心复制 API Key"
en = "Copy your API key from your account at open.bigmodel.cn"

[secret.verify]
url = "https://open.bigmodel.cn/api/anthropic/v1/messages"

[api]
base_url = "https://open.bigmodel.cn/api/anthropic"
wire = "anthropic"
