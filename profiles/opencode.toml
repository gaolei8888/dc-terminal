name = "opencode"
command = ["opencode"]
is_agent = true
# TUI 没实测过，不填 pattern —— 状态显示「—」比编一个假状态好

[label]
zh = "OpenCode"
en = "OpenCode"

[note]
zh = "开源，可接多种模型"
en = "Open source, works with many models"

[install]
command = ["npm", "i", "-g", "opencode-ai"]

[install.note]
zh = "需要先装 Node.js"
en = "Requires Node.js"
