这条提交的消息是个占位符（wip），提交本身是两件独立的事：

- `dct restart` 换掉在跑的守护进程（`cli::run_restart`）。所有会话跟着断，
  所以默认先问一句，`-y` 免问；跟 ps/stop 一样不会顺手拉起一个。
- 看板上当前项目戴一个 chip：组行上一个，标题栏上一个。chip 贴着名字走而
  不是撑满整列，也不许把名字那一列挤宽；断连时标题要同时放得下项目名和那
  句警告，CJK 名字被 chip 占掉宽度之后还要给失败计数留出位置。文件夹已经
  不在的组不戴 chip。

另外顺带补了一处上游的编译错误：b57ac5a 给 Profile 加了 backend_only，漏改
session.rs 的测试构造，origin/main 上 `cargo test` 因此编译不过。

（消息没改成这个，是因为这条提交已经推出去了，改它要重写历史。这份说明用
git notes 附上，不动提交本身。）
